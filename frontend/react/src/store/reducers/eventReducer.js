const initialState = {
	selectedEvent: null,
	eventId: null,
	eventDetails: null,
	allEvents: [],
};

const eventReducer = (state = initialState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};

export default eventReducer;
